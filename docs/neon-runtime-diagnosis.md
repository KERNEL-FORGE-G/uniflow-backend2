Le 14 août 2026, les endpoints publics du backend personnel /api/v1/subscription/plans, /api/v1/subscription/pricing et /api/v1/auth/academic-options renvoient HTTP 500, tandis que / et /api/docs-json renvoient HTTP 200. Cela indique que le bootstrap NestJS fonctionne mais qu’une requête dépendante de Prisma échoue en production. Le repository ne fournit pas de DATABASE_URL localement ; PrismaService utilise actuellement uniquement process.env.DATABASE_URL. Cette note est conservée avant correction pour éviter de perdre le diagnostic.
